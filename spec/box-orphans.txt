====== Box Model Features With No Home Specification ======

  * margin-collapse control (auto, discard-at-breaks, never-collapse, discard)

====== Run-in Use-cases ======

  * making <dt>s inline in the following <dd> (similar to how inline list markers work)
  * "small" (inside body content?) headings, which shouldn't carry too much visual weight to avoid distration